# No LLM/AI in the product, including menu voting

The product uses no LLM/AI at runtime — not for slip handling, summaries, nor the monthly menu
voting (Phase 6), which tallies votes with plain database logic. Recorded because an AI-assisted
suggestion feature is an obvious thing a future contributor might add; it is intentionally out of
scope to avoid API cost and operational complexity for a small office tool.
