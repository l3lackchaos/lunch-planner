"use client";

/**
 * STUB — foundation placeholder. Agent B (Auth/LIFF) replaces the body with the
 * real LIFF init + LINE login + session bootstrap. Keep the component name and
 * default export stable so src/app/layout.tsx keeps compiling.
 */
export default function LiffProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
